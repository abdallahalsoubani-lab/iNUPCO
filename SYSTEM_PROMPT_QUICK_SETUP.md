# ⚡ إعداد سريع - System Prompt النجدي

## الطريقة الأسهل والأسرع

### 1️⃣ افتح ملف .env.local

```bash
nano .env.local
# أو
code .env.local
```

### 2️⃣ انسخ System Prompt

**الخيار A: النص المختصر (للتجربة)**

```env
OPENAI_API_KEY=sk-your-key-here
SYSTEM_PROMPT=أنت مساعد نوبكو الرقمي. تتكلم باللهجة النجدية الرسمية وتساعد المستخدمين بمعلومات موقع نوبكو وبواباته. لا تخترع معلومات غير متاحة. استخدم: "ما قدرّت أتأكد من هالمعلومة من المصادر المتاحة عندي" عند عدم التأكد. وجّه المستخدمين للبوابات المناسبة (inupco.nupco.com، tenders.nupco.com، nupcocare.nupco.com). اختم كل رد بسؤال متابعة.
```

**الخيار B: النص الكامل (للإنتاج) - مُوصى به**

1. افتح `system-prompt.txt`
2. انسخ المحتوى **كاملاً**
3. الصق في `.env.local` بهذا الشكل:

```env
OPENAI_API_KEY=sk-your-key-here
SYSTEM_PROMPT="# System Prompt – مساعد نوبكو الذكي (لهجة نجدية رسمية)

أنت مساعد دردشة مخصص للإجابة عن موقع/بوابات نوبكو ومحتواها.

[...الصق باقي النص هنا...]

**نهاية System Prompt**"
```

> **ملاحظة:** ضع النص كله داخل علامات تنصيص مزدوجة `"..."`

### 3️⃣ احفظ وأعد تشغيل

```bash
# أعد تشغيل السيرفر
./run.sh dev
```

---

## ✅ التأكد من النجاح

افتح Chat Widget وجرّب:

**اكتب:** "وشلون أسجل كمورد؟"

**يجب أن يرد بـ:**
```
## تسجيل مورد جديد

**الخطوات:**
1. ادخل على بوابة تسجيل الموردين iNUPCO.
...

---
وش نوع نشاط شركتك؟
```

إذا رد بهذا الأسلوب → **نجح!** ✅

---

## 🐛 استكشاف الأخطاء

### المشكلة: الرد بالإنجليزية أو بدون لهجة نجدية

**الحل:** تأكد من:
1. نسخت System Prompt بشكل صحيح
2. حفظت ملف `.env.local`
3. أعدت تشغيل السيرفر (`Ctrl+C` ثم `./run.sh dev`)

### المشكلة: خطأ في parsing

**الحل:** استخدم علامات تنصيص:
```env
SYSTEM_PROMPT="النص هنا..."
```

### المشكلة: النص طويل جداً

**الحل:** استخدم النص المختصر أولاً، أو استخدم طريقة التحميل من ملف (راجع SYSTEM_PROMPT_GUIDE.md)

---

## 📚 للمزيد من التفاصيل

- **النص الكامل:** `system-prompt.txt`
- **الدليل الشامل:** `SYSTEM_PROMPT_GUIDE.md`
- **التخصيص:** راجع SYSTEM_PROMPT_GUIDE.md → قسم "التخصيص"

---

**جاهز؟ جرّب الآن! 🚀**
